# 工作记录：微信 4.1 语音通话自动化（2026-08-31）

> 环境背景：Windows 10 Pro 19045 / 微信 4.1.2.17 / Python 3.13（`.venv`，uv 管理）/ NVDA 便携版常驻激活（见 `工作记录-2026-08-17.md`）
> 需求：**机器人自动拨打语音电话，对方接通后自动挂断**（实测对象：好友「小康」，微信号 crypto_kang）
> 结论：拨出 ✅ / 接通 ✅ / 红钮自动挂断 ✅，全链路已实战验证；「脚本自主检测接通」未完成（见遗留问题）

---

## 一、能力盘点（调查结论）

- `pyweixin/WeChatAuto.py:222` 的 `Call` 类自带 `voice_call(friend)` / `video_call(friend)`：打开好友聊天窗口 → 点击右上角工具栏「语音聊天」按钮。`friend` 参数须用**好友备注/昵称**（搜索结果按显示名精确匹配），不是微信号。
- `pyweixin/Uielements.py:321-326` 定义的通话窗口类名 `ILinkAudioWnd / AudioWnd / VoipWnd / ILinkVoipWnd` **在本机 4.1.2.17 上从未出现过**，按它找通话窗口必然失败。
- `wx_sender_server.py`（:15000 发送服务）**没有任何通话接口**，仅 4 个纯文本端点（`/health`、`/targets`、`/api/{wxid}/send_text`、`/send`）。通话须在本机直跑脚本。

## 二、踩坑过程（v3 → v5，三次迭代）

| 版本 | 现象 | 根因 |
|---|---|---|
| v3 | 「点击后出现新窗口」以为拨出，90s「无人接听」，取消失败 | 那个新窗口根本是**下拉菜单**，从未真正拨出；菜单 UIA 树为空找不到取消按钮 |
| v4 | 误报「已接通！计时 10:05」；红钮检测失败 | 接通判据 `\d{1,2}:\d{2}` 扫全部可见窗口，**误匹配聊天记录时间戳**；同上，菜单里没有红钮 |
| v5 | 拨出成功（人工确认来电已到被叫端），但监控循环拨出 8s 后即退出 | 通话窗口**在对方接听后才作为可见顶层窗口出现**，拨出瞬间快照抓不到 |

## 三、核心根因与机制（重要，别再踩）

1. **两段式拨出**：点击聊天窗口「语音通话」按钮后，弹出的是「语音通话 / 视频通话」**下拉菜单**（win32 class=`Qt51514QWindowToolSaveBits`，title=`Weixin`，约 330×145，两选项）。**必须在菜单里再点一次「语音通话」菜单项**（约窗口 50% 宽、28% 高处）才真正拨出。
2. **菜单/通话窗口 UIA 均为空**（descendants≈2，即使 NVDA 已注入、主窗口 188 控件正常）——`child_window` 定位法对它们无效，只能 win32 `GetWindowRect` + 坐标点击。
3. **窗口时序**：菜单（拨出瞬间）→ 等待期 UI（不可见顶层窗口）→ **对方接听后才出现可见顶层通话窗口**（仍是 `Qt51514QWindowToolSaveBits` 系）。拒接/挂断后窗口自行销毁；无人接听微信约 2 分钟自动结束（兜底）。
4. **挂断方案（已实战验证）**：通话界面挂断/取消钮为红色圆钮 → 截图窗口 → 像素聚类找红钮（判据 `r>140, g<90, b<90`，隔行采样，≥30 点取均值中心）→ `pyautogui.click` 全局坐标。实测通话中红钮位于屏幕 (959,755)，点击后通话窗口立即消失。
5. **DPI 大坑**：截图/取窗口/点击混用前必须 `ctypes.windll.user32.SetProcessDPIAware()`，否则 `GetWindowRect` 被 DPI 虚拟化，与 `ImageGrab`（物理像素）错位，红钮坐标必偏。
6. 杂项：pywinauto 元素类型用 `e.element_info.control_type`（wrapper 没有 `control_type()` 方法）；接通判据勿用宽正则扫时间文本。

## 四、最终脚本（仓库根目录）

### `call_xiaokang.py`（v5）—— 拨出 + 监控

```
PYTHONUTF8=1 .venv/Scripts/python.exe call_xiaokang.py
```

流程：打开「小康」聊天 → 点「语音通话」按钮 → 截图确认菜单（`debug_shots/*_menu.png`）→ 坐标点菜单第一项 → 全屏截图（`*_dialed.png`）→ 监控循环每 4s 截 `debug_shots/live_call.png` + 截图哈希变化检测 → 150s 超时自动找红钮取消。
内置 `GlobalConfig.close_weixin = False`（wx_sender_server 常驻依赖微信，绝不能关）。

### `call_hangup.py` —— 独立挂断（可随时手动触发）

```
PYTHONUTF8=1 .venv/Scripts/python.exe call_hangup.py
```

遍历微信可见窗口（非主窗口优先、主窗口兜底），像素找红钮坐标点击。2026-08-31 实战：真实通话中成功定位并点击，通话随即结束，无残留窗口。

## 五、实测时间线（2026-08-31）

| 时间 | 事件 |
|---|---|
| 12:08 | v3 首跑：误以为拨出（实为菜单弹出），90s 后微信自行超时 |
| 12:29 | v4：菜单误判 + 时间戳误匹配「已接通」，挂断失败 |
| 12:31 | v5 修正两段式拨出：**真正拨出成功**，被叫端拒接，窗口自行消失 |
| 12:32-12:33 | v5 再拨：拨出成功 → 被叫端接听（通话建立）→ `call_hangup.py` 红钮点击挂断成功，微信恢复干净 |

## 六、遗留问题（v6 待办）

- **脚本自主检测「已接通」未实现**：v5 监控在接听前退出（见踩坑表）。v6 思路：监控循环每轮**重新扫描**可见非主微信窗口（而非只用拨出瞬间的窗口集），发现带红钮的窗口后用截图哈希变化率区分「等待（静止）」与「通话中（计时数字每秒跳动）」，确认后自动点红钮。
- 挂断按钮视觉定位依赖红色像素，若微信改版换色/换样式需重新标定。
- `wx_sender_server.py` 未暴露通话 HTTP 接口；如需远程触发拨打，可加 `POST /api/{wxid}/voice_call`（复用上述两脚本逻辑）。

---

# StarBot 通道迁移：wx_sender 升级为双向收发（2026-08-31 下午）

## 背景

coinMarker 的 StarBot 个人微信协议接口（frpc `starbot-api` 18000 + 回调 19600）停用，
个人微信通道整体切到本项目。策略：**协议兼容**——wx_listener 直接模拟 StarBot 回调
JSON 格式（`event="10002"`），coinMarker 侧 server/handler/command 零逻辑改动。

## 通信拓扑（用户确认：推送回调模式）

- 发送：coinMarker → `127.0.0.1:15000`（frpc `wx` 隧道，wx_sender_server 原有链路）
- 接收：wx_listener 轮询白名单好友独立窗口 → POST 公网
  `http://91.98.134.93:19600/wcf/callback`（token 鉴权，服务器 ufw 放行 19600）

## 新增/改动文件

| 文件 | 说明 |
|---|---|
| `wx_common.py`（新） | `UIA_LOCK` 全局互斥（微信 UIA 单线程，收发必须串行）+ `record_sent_intent`/`is_recently_sent` 自回环过滤（先登记后发送消除竞态，15min TTL 归一化匹配） |
| `wx_listener.py`（新） | `WhitelistListener`（独立窗口 runtime_id 增量轮询，TAIL_SCAN=5 吸收 burst，watchdog 60s 限频重开，重开后尾部记已见不转发历史）+ `CallbackWorker`（StarBot 10002/10001 格式转发，退避 2/4/8s×3，登录事件先发） |
| `wx_sender_server.py` | SendWorker 持 UIA_LOCK + 发送前登记自回环；job 加 wxid 字段；/health 加 listener_status/callback_status；DEFAULT_CONFIG 加 listener/callback section + 白名单 targets |

## 关键设计决策（为什么不选其他方案）

1. **收消息用独立窗口 runtime_id 监听**，不用 `check_new_messages()`：
   后者靠红点徽标（清零有竞态、排除免打扰会话）+ `pull_messages` 取"最后 N 条"
   **不区分收发双方**（自己的回复会被当新消息回传→自回环）
2. **不用 AutoReply.auto_reply_to_friend**：同步回调直接在窗口内键鼠回复，
   绕过发送队列；且其 `is_my_bubble` 调用 3 参 vs 定义 2 参（潜在 TypeError）
3. **不引入统一调度线程**：保留 SendWorker 队列消费线程，所有 pyweixin 调用点
   （发送/开窗/轮询读）统一 `with UIA_LOCK:`，锁粒度=单次完整操作，
   节流 sleep 在锁外保持原语义

## 待人工填写的配置（wx_sender_config.json）

- `listener.friends`：3 位白名单好友的**完整精确备注名**（开窗搜索与回复寻址都用名字）
- `targets`：补齐 3 位好友私聊条目 + VolumeBreakout 群（`PYWX_VOLUME_BREAKOUT_GROUP_ID`
  对应 chatroom id）的 name
- `callback.token`：与服务器 `PYWX_CALLBACK_TOKEN` 一致

## 部署注意

- ⚠️ frps 远程 15000 同名代理冲突：coinMarker 仓库 frpc.toml 的 `wx`（家庭端）必须先停
- 验证链路：白名单手机发 `帮助` → 收到命令说明；`/health` 看 listener_status；
  服务器 `curl 127.0.0.1:19600/wcf/health` 应 `{"sessions":1}`
- 详见 `DEPLOY_WX_SENDER.md` 步骤 7
