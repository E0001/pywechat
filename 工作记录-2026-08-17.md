# 工作记录：微信/企业微信 UI 自动化（2026-08-17）

> 环境背景：Windows 11 / 微信 4.1.12.55 / 企业微信 5.0.9.6029 / Python 3.13（uv 管理，`.venv` 可编辑安装 pywechat）
> 需求：**自动发送消息**（读取走微信本地数据库，已另行解决）

---

## 一、微信 4.1.12 发送自动化 —— ✅ 已打通

### 1.1 问题

`pyweixin` 的 `Messages.send_messages_to_friend` 报 `NotFoundError: 无法识别定位到微信主界面`。根因：微信 4.x 的 mmui 框架（定制 Qt）**UIA 控件树按需暴露**——默认只给屏幕阅读器，普通 UIA 客户端只能看到 2 个空容器。

### 1.2 逆向结论（Weixin.dll 186MB，strings + capstone 静态分析）

微信检测读屏器的完整机制（代码位于 0x1808da2ac 附近）：

| 检测项 | 状态 | 说明 |
|---|---|---|
| 注册表 `HKCU\Software\Microsoft\Narrator\NoRoam\RunningState` | 活代码 | 讲述人运行时=1，**弱信号** |
| NVDA DLL 枚举 | 活代码 | 检查进程是否加载 `nvdahelperremote.dll` 等 5 个 NVDA 组件，**强信号** |
| `clicfg_accessible_check_narrator` 配置 | 死字符串 | 4.1.12 未被 clicfg gate |

**版本行为差异**（关键）：
- 4.1.5.16 / 4.1.10.31：讲述人即可激活主窗口（知乎文章、pywechat issue #270 实证）
- **4.1.12.55（本机）**：讲述人只能激活**登录窗口**，主窗口需 NVDA DLL 注入（实测：讲述人穷尽各种姿势均失败，NVDA 注入成功）

### 1.3 最终方案：NVDA 便携版激活（已实测验证）

**NVDA 是什么**：NonVisual Desktop Access，免费开源 Windows 屏幕阅读器（与英伟达无关，纯缩写撞车）。不需要显卡，CPU 运行约 200MB 内存。我们利用的正是它对目标进程的 DLL 注入——微信把 NVDA 用户当视障用户，为无障碍暴露完整 UI 树。

**资源位置**：
- NVDA 2026.1.1 便携版：`D:\VibeCoding\nvda\portable\`（安装包 `D:\VibeCoding\nvda\nvda_2026.1.1.exe`）
- 下载源：GitHub `nvaccess/nvda` release-2026.1.1（本机走代理 `127.0.0.1:7897`）

**激活流程（仅微信重启后需要）**：
1. NVDA 在微信启动**之前**运行：`D:\VibeCoding\nvda\portable\nvda.exe --no-speech -m`（静音、无弹窗；退出用 `nvda.exe -q`）
2. 关闭微信：`taskkill /IM Weixin.exe /F`（+ WeChatAppEx.exe）
3. 启动微信，登录窗口出现后**把窗口带到前台**（点击它即可）——NVDA 只注入前台焦点窗口的进程，注入 `nvdaHelperRemote.dll` 后微信检测到
4. 扫码登录 → 主窗口创建时已带注入状态 → UIA 完整暴露

**验证**：主窗口 `descendants()` 返回 88 个控件（导航栏、`Edit "搜索"`、`List "会话"`、ChatDetailView 等）；`send_messages_to_friend('文件传输助手', [...])` 发送成功。

**注意**：
- 主窗口第一层 `children()` 只有 2 个是正常的（第一层是 QWidget 容器），完整树用 `descendants()`
- 激活后 NVDA 可一直静音挂着，微信不重启就一直有效
- 微信自动更新后机制可能变化，届时重跑验证脚本

### 1.4 发送消息用法

```python
from pyweixin import Messages
from pyweixin.Config import GlobalConfig

GlobalConfig.close_weixin = False   # 重要：防止任务后关闭微信
Messages.send_messages_to_friend(
    friend='文件传输助手',
    messages=['消息1', '消息2'],
    close_weixin=False,
)
```

---

## 二、企业微信 pywxwork —— ⏸️ 暂停（骨架已建，待校准）

### 2.1 需求与前提

- 目标：向**外部群（客户群）**发送消息——外部群无 API 通道；企业认证 API 300 元/次且群发易封号 → 只能走 UI 自动化
- 企业微信 5.0.9.6029，安装于 `C:\Program Files (x86)\WXWork\`（**32 位**）

### 2.2 调研结论（全线排除 UIA/MSAA/逆向路线）

| 路线 | 结论 | 依据 |
|---|---|---|
| UIA | ❌ **根本未实现** | WXWork.exe 导入表无 `uiautomationcore.dll`；主窗口 `WeWorkWindow`（自研 Perry 框架，基于 duilib.dll+skia 自绘）UIA 树恒为 2 个空容器 |
| MSAA | ❌ 同上 | `WM_GETOBJECT` 对所有窗口返回 0，无 oleacc.dll |
| NVDA 注入 | ❌ 无效 | DLL 确实注入了进程（3 个 DLL），UIA 树不变——因为压根没有 Provider 可激活 |
| Chromium 无障碍 | ❌ 不覆盖聊天 | WXWorkWeb 进程的 CSS/JS 资源全是文档编辑器/报表组件，主聊天界面是 duilib 原生自绘 |
| 布局 XML 提取 | ❌ | 资源段仅 1 个 171B 的 duilib 布局，主界面布局 C++ 硬编码 |
| DLL 注入内部函数 | 🚫 已否决 | 规避客户端完整性检测（wintrust/crypt32 签名校验、anr.exe 异常上报）违反许可协议；且服务端风控看行为特征，企业账号有连带风险；32 位+版本绑定维护成本高 |

**坐标方案的正当性**：pyautogui/SendInput 产生的是操作系统层**真实输入事件**，与真人敲键盘同路，进程零修改零注入零协议接触——不是"藏起来"，而是天然无法区分。

### 2.3 已交付：pywxwork 包骨架（未校准未实测）

```
pywxwork/
├── __init__.py   # from pywxwork import Messages
├── Config.py     # GlobalConfig: close_wxwork/send_delay/input_box_x_ratio(0.55)/input_box_y_offset(70)
├── Tools.py      # 窗口定位(WeWorkWindow)/DPI感知/剪贴板/前台化/输入框点击
└── Messages.py   # send_messages_to_group(group_name, messages)
```

流程：`Ctrl+F` 全局搜索 → 剪贴板粘贴群名 → Enter 选第一个结果 → 点击输入框（窗口相对坐标）→ 逐条粘贴 → Enter 发送。

### 2.4 待办（恢复时从这里继续）

1. 校准：确认 Ctrl+F 是否聚焦搜索框（`probe_step1.py` 已按过一次未确认结果）
2. 校准输入框点击坐标（实测窗口 1000×888，计算点击点 (706,1025)）
3. 用户提供测试群名 → 实测 `send_messages_to_group`
4. 改进：消息间隔随机化（1.2s±0.5s 拟人化）、发送结果截图验证（可选 OCR）

---

## 三、文件清单

| 路径 | 说明 |
|---|---|
| `D:\VibeCoding\nvda\portable\` | NVDA 2026.1.1 便携版（微信激活用） |
| `send_test.py` | 微信发送测试（文件传输助手）✅ 已通过 |
| `verify_uia.py` | 检查微信窗口 UIA + NVDA 注入状态 |
| `deep_dump.py` | 微信主窗口 UIA 深度遍历 |
| `wait_login_verify.py` | 微信启动→等登录→验证全流程 |
| `pywxwork/` | 企业微信自动化包（待校准） |
| `probe_step1.py` | 企业微信搜索快捷键校准脚本 |
| `rev_imports.py` / `rev_resources.py` / `dump_webres.py` | 逆向分析脚本 |
| `debug_shots/` | 截图与 dump 的 Web 资源（可删） |

其余一次性实验脚本（`uia_setup*.py`、`nvda_activate.py`、`wxwork_*.py`、`*narrator*.py` 等）为过程产物，可删可留。

> 当前状态：讲述人已关闭；NVDA 若仍在后台可随时 `nvda.exe -q` 退出（微信不重启则 UIA 激活状态不受影响）。

---

## 五、wx_sender 快讯推送服务 — 新机部署完成 ✅（同日傍晚）

全链路已贯通并经服务器侧验证：coinMarker(91.98.134.93) → frps:17777 → frpc(本机,开机自启) → :15000 wx_sender → pyweixin UIA → 微信群「快讯」。

| 组件 | 本机状态 |
|---|---|
| wx_sender 服务 | 运行中，9/9 targets 全部映射群「快讯」（JSSZ 目标 at_all=true） |
| NVDA | C:\Users\kk\nvda\portable（start_wx_sender.bat 已改此路径） |
| frpc | C:\Users\kk\Desktop\frpc\frpc.toml（含 wx 隧道 15000，token 真实值，frpc_hidden.vbs 开机自启） |
| 电源策略 | 睡眠/休眠/关屏全部"永不"（已确认） |

**本机部署中遇到的坑（DEPLOY_WX_SENDER.md 未覆盖）**：
1. 从旧机拷贝文件时把旧机 `.venv` 一起覆盖 → uv trampoline 报错。修复：`rm -rf .venv && uv venv --python 3.13 && uv pip install -e .`。**拷文件勿带 .venv**
2. `start_wx_sender.bat` 从 git-bash/cmd 包装启动会 PATH 污染（`find` 解析成 Unix find）+ 中文编码问题 → 服务起不来。**直接双击 bat 或直接跑 `python wx_sender_server.py`**
3. RDP 断开必锁会话 → UIA 发送失效。解法：断开前管理员运行 `C:\Users\kk\Desktop\disconnect_keep_alive.bat`（tscon /dest:console，桌面保持解锁）
4. Windows 更新无活跃时间设置，可能任意时刻重启 → 重启后需人工恢复（NVDA→微信扫码→start_wx_sender.bat→frpc 自启）

**日常运维**：
- 状态：`curl http://127.0.0.1:15000/health`（看 sent_total/failed_total/last_error）
- 日志：`wx_sender.log`（2MB×3 轮转），入队/发送/失败都有记录
- 改群名映射：直接编辑 `wx_sender_config.json`，无需重启
- 排队机制：单线程串行 + 每条 3-6s 随机延迟（≈10s/条），队列上限 200 满则丢弃
