__pycache__/
*.pyc
*.venv

# wx_sender 运行产物（含消息内容，禁止入库）
wx_sender.log*
wx_sender_config.json

# 隐私截图与本地环境文件（截图含聊天内容/头像，frpc 含隧道配置）
debug_shots/
debug_*.png
frpc-wx.toml

# 构建与运行产物
*.egg-info/
courgette.log

# wx_forwarder 运行产物（含回调 token，禁止入库）
wx_forwarder.log*
wx_forwarder_config.json

# wx_listener 运行产物（echo 文件含消息内容，禁止入库）
wx_listener.log*
wx_sent_echo.jsonl
wx_inbound_seen.jsonl
.lock-*
rdp_keepalive.log

# 运行时注册表缓存(自动重建)
wx_contact_names.json
wx_chat_windows.json
